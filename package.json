{
  "name": "backend-frontend",
  "version": "0.1.0",
  "description": "This collection package integrates the server and client for deployment.",
  "main": "server/app.js",
  "scripts": {
    "postinstall": "(cd server && npm install); (cd client && npm install && npm install --dev && npm run build)"
  }
}
