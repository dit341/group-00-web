{
  "name": "backend-frontend",
  "version": "0.1.0",
  "description": "This collection package integrates the server and client for deployment.",
  "main": "server/app.js",
  "scripts": {
    "postinstall": "(cd server && npm install); (cd client && npm install && VUE_APP_API_ENDPOINT=http://localhost:${PORT:3000}/api npm run build)"
  }
}
